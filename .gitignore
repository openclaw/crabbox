bin/
dist/
dist-cloudflare/
/worker/dist-node/
artifacts/
.crabbox/
.clawpatch/
node_modules/
.wrangler/
*.test
coverage.out
/crabbox
.gomu_history.json
mutation-report.*
reports/mutation/

# Local env files: never commit API keys, tokens, or user secrets.
*.env
api.env*

# Local planning artifacts from $prepare-plans / $prepare-plans-goals.
# These are scratch inputs for the current session, not part of the repo.
PLAN*.md
plan*.md
