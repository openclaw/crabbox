# --- Xcode / XcodeGen ---------------------------------------------------------
# The .xcodeproj is generated by `xcodegen generate` from project.yml — never commit it.
*.xcodeproj/
DerivedData/
*.xcuserstate
xcuserdata/

# --- Swift Package Manager ----------------------------------------------------
.build/
.swiftpm/
Package.resolved

# --- Signing / secrets --------------------------------------------------------
# App Store Connect API keys (.p8) and signing certs/profiles (.p12) must stay local.
*.p8
*.p12

# --- macOS --------------------------------------------------------------------
.DS_Store
